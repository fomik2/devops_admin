---
- set_fact: 
    primary_dns: "{{ ansible_default_ipv4.address }}"
    zone_name: "fffff"

- name: test
  template:
    src: forward_zone.j2
    dest: /home/admin/vagrant_box/{{ forward_zone_name }}.conf

- name: test
  template:
    src: reverse_zone.j2
    dest: /home/admin/vagrant_box/{{ reverse_zone_name }}.conf



- name: test
  template:
    src: named.j2
    dest: /home/admin/vagrant_box/named.conf

  
